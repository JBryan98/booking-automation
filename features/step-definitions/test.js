let array = ["Cusco", "awssdasda", "asassaadad", "Cusco"]

console.log(array.find(arr => arr === "Cusco"))

array.forEach(arr => {
    if(arr === "Cusco"){
        console.log(arr)
       // arr.click()
    }
})